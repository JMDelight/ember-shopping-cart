import { cartItemQuantity } from 'marketplace/helpers/cart-item-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | cart item quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartItemQuantity([42]);
  assert.ok(result);
});
